#include <iostream>
#include <fstream>

#include "Lexer.hpp"
#include "Parser.hpp"
#include "Token.hpp"

using namespace std;

int main() {
	std::ifstream file("proba.txt");
	Parser p(&file);

        Node *tree = p.parse();

	cout << "The tree: " << endl;
	cout << tree->to_string() << "." << endl;
	
	return 0;
}
